# CAT Merging: Context-Aware Task Merging

This repository contains the implementation for merging two expert models (Reasoning and Medical) into a Base Llama model using Task Vector Trimming.

## 🚀 Usage

The project is structured into a Bash configuration script and a Python execution script.

1. **Permissions**: Ensure the script is executable.
   ```bash
   chmod +x run_cat_merge.sh